import React from 'react';
import { Text, View } from 'react-native';

const HomeScreen = ({ imgSrc,text }) => {
    return (
        <View>
            <Text>Home Screen</Text>
        </View>
    );
}

export default HomeScreen;